services:
  - type: web
    name: pr-review-ai-agent
    env: python
    plan: free
    rootDir: Api
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
 